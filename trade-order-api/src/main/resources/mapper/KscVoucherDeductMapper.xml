<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ksyun.trade.mapper.KscVoucherDeductMapper">

    <insert id="insert" parameterType="com.ksyun.trade.pojo.KscVoucherDeduct">
        INSERT INTO ksc_voucher_deduct (id, order_id, voucher_no, amount, before_deduct_amount, after_deduct_amount)
        values (#{id}, #{orderId}, #{voucherNo}, #{amount}, #{beforeDeductAmount}, #{afterDeductAmount})
    </insert>
</mapper>
